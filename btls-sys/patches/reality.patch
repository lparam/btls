diff --git a/include/openssl/ssl.h b/include/openssl/ssl.h
index ff68ba69e..178011a4e 100644
--- a/include/openssl/ssl.h
+++ b/include/openssl/ssl.h
@@ -5786,6 +5786,9 @@ OPENSSL_EXPORT int SSL_CTX_set_tmp_ecdh(SSL_CTX *ctx, const EC_KEY *ec_key);
 // |ec_key|'s curve. The remainder of |ec_key| is ignored.
 OPENSSL_EXPORT int SSL_set_tmp_ecdh(SSL *ssl, const EC_KEY *ec_key);
 
+// REALITY support
+OPENSSL_EXPORT void SSL_set_fixed_ecdh_private_key(SSL *ssl, const uint8_t *key, size_t key_len);
+
 #if !defined(OPENSSL_NO_FILESYSTEM)
 // SSL_add_dir_cert_subjects_to_stack lists files in directory |dir|. It calls
 // |SSL_add_file_cert_subjects_to_stack| on each file and returns one on success
diff --git a/ssl/extensions.cc b/ssl/extensions.cc
index c5f90688c..25ed0778c 100644
--- a/ssl/extensions.cc
+++ b/ssl/extensions.cc
@@ -2276,6 +2276,9 @@ bool ssl_setup_key_shares(SSL_HANDSHAKE *hs, uint16_t override_group_id) {
   CBB key_exchange;
   for (const uint16_t group_id : *selected_key_shares) {
     UniquePtr<SSLKeyShare> key_share = SSLKeyShare::Create(group_id);
+    if (key_share != nullptr && (group_id == SSL_GROUP_X25519 || group_id == SSL_GROUP_X25519_KYBER768_DRAFT00 || group_id == SSL_GROUP_X25519_MLKEM768) && hs->config->has_fixed_x25519_key) {
+      key_share->SetFixedPrivateKey(hs->config->fixed_x25519_key);
+    }
     if (key_share == nullptr ||                                    //
         !CBB_add_u16(cbb.get(), group_id) ||                       //
         !CBB_add_u16_length_prefixed(cbb.get(), &key_exchange) ||  //
diff --git a/ssl/internal.h b/ssl/internal.h
index a69505b47..66555baef 100644
--- a/ssl/internal.h
+++ b/ssl/internal.h
@@ -918,6 +918,7 @@ class SSLKeyShare {
 
   // GroupID returns the group ID.
   virtual uint16_t GroupID() const = 0;
+  virtual bool SetFixedPrivateKey(bssl::Span<const uint8_t> key) { return false; }
 
   // Generate generates a keypair and writes the public key to |out_public_key|.
   // It returns true on success and false on error.
@@ -3227,6 +3228,9 @@ struct ALPSConfig {
 // See SSL_shed_handshake_config() for more about the conditions under which
 // configuration can be shed.
 struct SSL_CONFIG {
+  // REALITY fixed key support
+  bool has_fixed_x25519_key = false;
+  uint8_t fixed_x25519_key[32] = {0};
   static constexpr bool kAllowUniquePtr = true;
 
   explicit SSL_CONFIG(SSL *ssl_arg);
diff --git a/ssl/ssl_key_share.cc b/ssl/ssl_key_share.cc
index d155b5527..ec7ff0324 100644
--- a/ssl/ssl_key_share.cc
+++ b/ssl/ssl_key_share.cc
@@ -138,6 +138,16 @@ class ECKeyShare : public SSLKeyShare {
 };
 
 class X25519KeyShare : public SSLKeyShare {
+ public:
+  bool has_fixed_key_ = false;
+
+  bool SetFixedPrivateKey(bssl::Span<const uint8_t> key) override {
+    if (key.size() != 32) return false;
+    OPENSSL_memcpy(private_key_, key.data(), 32);
+    has_fixed_key_ = true;
+    return true;
+  }
+
  public:
   X25519KeyShare() {}
 
@@ -145,7 +155,7 @@ class X25519KeyShare : public SSLKeyShare {
 
   bool Generate(CBB *out) override {
     uint8_t public_key[32];
-    X25519_keypair(public_key, private_key_);
+    if (has_fixed_key_) { X25519_public_from_private(public_key, private_key_); } else { X25519_keypair(public_key, private_key_); }
     return !!CBB_add_bytes(out, public_key, sizeof(public_key));
   }
 
@@ -195,6 +205,16 @@ class X25519KeyShare : public SSLKeyShare {
 
 // draft-tls-westerbaan-xyber768d00-03
 class X25519Kyber768KeyShare : public SSLKeyShare {
+ public:
+  bool has_fixed_key_ = false;
+
+  bool SetFixedPrivateKey(bssl::Span<const uint8_t> key) override {
+    if (key.size() != 32) return false;
+    OPENSSL_memcpy(x25519_private_key_, key.data(), 32);
+    has_fixed_key_ = true;
+    return true;
+  }
+
  public:
   X25519Kyber768KeyShare() {}
 
@@ -204,7 +224,7 @@ class X25519Kyber768KeyShare : public SSLKeyShare {
 
   bool Generate(CBB *out) override {
     uint8_t x25519_public_key[32];
-    X25519_keypair(x25519_public_key, x25519_private_key_);
+    if (has_fixed_key_) { X25519_public_from_private(x25519_public_key, x25519_private_key_); } else { X25519_keypair(x25519_public_key, x25519_private_key_); }
 
     uint8_t kyber_public_key[KYBER_PUBLIC_KEY_BYTES];
     KYBER_generate_key(kyber_public_key, &kyber_private_key_);
@@ -225,7 +245,7 @@ class X25519Kyber768KeyShare : public SSLKeyShare {
     }
 
     uint8_t x25519_public_key[32];
-    X25519_keypair(x25519_public_key, x25519_private_key_);
+    if (has_fixed_key_) { X25519_public_from_private(x25519_public_key, x25519_private_key_); } else { X25519_keypair(x25519_public_key, x25519_private_key_); }
     KYBER_public_key peer_kyber_pub;
     CBS peer_key_cbs, peer_x25519_cbs, peer_kyber_cbs;
     CBS_init(&peer_key_cbs, peer_key.data(), peer_key.size());
@@ -284,6 +304,16 @@ class X25519Kyber768KeyShare : public SSLKeyShare {
 
 // draft-ietf-tls-ecdhe-mlkem-00
 class X25519MLKEM768KeyShare : public SSLKeyShare {
+ public:
+  bool has_fixed_key_ = false;
+
+  bool SetFixedPrivateKey(bssl::Span<const uint8_t> key) override {
+    if (key.size() != 32) return false;
+    OPENSSL_memcpy(x25519_private_key_, key.data(), 32);
+    has_fixed_key_ = true;
+    return true;
+  }
+
  public:
   X25519MLKEM768KeyShare() {}
 
@@ -295,7 +325,7 @@ class X25519MLKEM768KeyShare : public SSLKeyShare {
                           &mlkem_private_key_);
 
     uint8_t x25519_public_key[X25519_PUBLIC_VALUE_LEN];
-    X25519_keypair(x25519_public_key, x25519_private_key_);
+    if (has_fixed_key_) { X25519_public_from_private(x25519_public_key, x25519_private_key_); } else { X25519_keypair(x25519_public_key, x25519_private_key_); }
 
     if (!CBB_add_bytes(out, mlkem_public_key, sizeof(mlkem_public_key)) ||
         !CBB_add_bytes(out, x25519_public_key, sizeof(x25519_public_key))) {
@@ -315,7 +345,7 @@ class X25519MLKEM768KeyShare : public SSLKeyShare {
 
     MLKEM768_public_key peer_mlkem_pub;
     uint8_t x25519_public_key[X25519_PUBLIC_VALUE_LEN];
-    X25519_keypair(x25519_public_key, x25519_private_key_);
+    if (has_fixed_key_) { X25519_public_from_private(x25519_public_key, x25519_private_key_); } else { X25519_keypair(x25519_public_key, x25519_private_key_); }
     CBS peer_key_cbs, peer_mlkem_cbs, peer_x25519_cbs;
     CBS_init(&peer_key_cbs, peer_key.data(), peer_key.size());
     if (!CBS_get_bytes(&peer_key_cbs, &peer_mlkem_cbs,
diff --git a/ssl/ssl_lib.cc b/ssl/ssl_lib.cc
index f64b103fb..0dfeefa94 100644
--- a/ssl/ssl_lib.cc
+++ b/ssl/ssl_lib.cc
@@ -3538,3 +3538,4 @@ int SSL_set1_requested_trust_anchors(SSL *ssl, const uint8_t *ids,
 }
 
 int SSL_CTX_get_security_level(const SSL_CTX *ctx) { return 0; }
+void SSL_set_fixed_ecdh_private_key(SSL *ssl, const uint8_t *key, size_t key_len) { if (!ssl || !ssl->config || key_len != 32) return; OPENSSL_memcpy(ssl->config->fixed_x25519_key, key, 32); ssl->config->has_fixed_x25519_key = true; }
